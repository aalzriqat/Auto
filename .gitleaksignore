# gitleaks fingerprints (commit:path:rule:line) allowlisted as HARMLESS, one per line.
# Keep this list to exact findings: unlike a line-targeted regex in .gitleaks.toml,
# a fingerprint cannot mask a real secret that later appears beside the fixture.
#
# scripts/zeroState.test.ts:200 — the identity fixture there sets the expected
# deployment to kindly-hound-172, a Convex DEPLOYMENT NAME
# (adjective-noun-number); generic-api-key matched it on entropy beside the
# word "credential". An address, not a credential.
28b4ce35639fc20a384e0d38736626370ba59bfd:scripts/zeroState.test.ts:generic-api-key:200
#
# aa12d3c97 .gitleaksignore:5 — this file's OWN explanatory comment, in the commit
# that introduced it, quoted the fixture verbatim and tripped the same rule; the
# comment was reworded in the next commit, but the historical blob is still in
# the scanned range. A comment about a deployment name, not a credential.
aa12d3c97d707fc8044123167f3dbe49e1ae01c2:.gitleaksignore:generic-api-key:5
#
# fd9b60038 convex/dealCustodyAccounting.test.ts:4172 — the R7-F5 test's
# restore line, which patches a ledger event's own outbox key back onto it
# after proving the family gate refuses a posted link under a key that is not
# the chain's; generic-api-key matched it on entropy beside the name of that
# key field. A field copied from the event itself, not a credential. (The
# line is described here, not quoted: a verbatim copy of it would trip the
# same rule on THIS file, as the aa12d3c97 entry above records.)
fd9b600387d145e607072200d7409e7af51027ac:convex/dealCustodyAccounting.test.ts:generic-api-key:4172
