import { MarketplaceSettingsClient } from "./client";

export const metadata = {
  title: "Dealer Network Marketplace | AutoFlow",
  description: "List your dealership on the AutoFlow buyer marketplace and receive car requests",
};

export default function MarketplaceSettingsPage() {
  return <MarketplaceSettingsClient />;
}
