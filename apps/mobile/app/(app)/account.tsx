import { BuyerAccountScreen } from "../../src/features/account/BuyerAccountScreen";

// Buyer account + the (visually secondary) dealer entry point.
export default function AccountRoute() {
  return <BuyerAccountScreen />;
}
